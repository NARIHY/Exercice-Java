import java.util.Scanner;
/*
 * Écrire un programme qui s'appelle PGCD qui demande à l'utilisateur de saisir
 *  deux entiers non nuls a et b puis qui affiche à l'écran le plus grand
 *   commun diviseur 
 *  (PGCD) de ceux-ci.
 */
public class Tp31 {
	public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        // Demander à l'utilisateur de saisir deux entiers non nuls a et b
        int a;
        do {
            System.out.print("Entrez le premier entier non nul a : ");
            a = sc.nextInt();
        } while (a == 0);

        int b;
        do {
            System.out.print("Entrez le deuxième entier non nul b : ");
            b = sc.nextInt();
        } while (b == 0);

        // Calculer et afficher le PGCD de a et b
        int pgcd = calculerPGCD(a, b);
        System.out.println("Le PGCD de " + a + " et " + b + " est : " + pgcd);
    }

    // Fonction pour calculer le PGCD de deux entiers
	/*
	 * tsy maintsy natao mitokana fa manao erreur eo amilay b
	 * Échange de valeurs : À chaque itération de la boucle, 
	 * les valeurs de b et a sont échangées. 
	 * Cela permet de maintenir la relation nécessaire pour l'algorithme d'Euclide (DIv ecludienne), où a devient b et b devient le reste de la division a par b.

Calcul du reste : À chaque itération, le nouveau reste de la division a par b est calculé et assigné à b. Cela répète le processus jusqu'à ce que b soit égal à zéro.

Retourner la valeur absolue : Une fois que la boucle est terminée, le PGCD est trouvé dans la variable a. Pour s'assurer que la valeur retournée est toujours positive, la méthode utilise Math.abs(a) pour renvoyer la valeur absolue de a.
	 */
    public static int calculerPGCD(int a, int b) {
        while (b != 0) {
            int temp = b;
            b = a % b;
            a = temp;
        }
        return Math.abs(a); // Le PGCD est toujours positif
    }

   
}
